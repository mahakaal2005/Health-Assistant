<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark theme overrides only - do not redefine base themes -->

    <!-- Dark mode specific optimizations for FullScreen theme -->
    <style name="Theme.Health_Assistant.FullScreen">
        <!-- Dark mode specific optimizations -->
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">false</item>
    </style>
    
    <!-- Dark theme overrides for Design System -->
    <style name="AppTheme">
        <!-- Design System Color Tokens (Dark Theme Overrides) -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>
        <item name="colorError">@color/colorError</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>
        <item name="colorOnError">@color/colorOnError</item>
    </style>
</resources>