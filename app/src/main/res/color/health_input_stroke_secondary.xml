<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state -->
    <item android:state_focused="true" android:color="@color/health_accent" />
    <!-- Error state -->
    <item android:state_activated="true" android:color="@color/error" />
    <!-- Default state -->
    <item android:color="@color/outline_variant" />
</selector>